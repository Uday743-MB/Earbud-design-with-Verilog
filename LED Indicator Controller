Design code 
module led_controller(
    input power_on,
    output reg led
);

always @(*)
begin
    if(power_on)
        led = 1'b1;
    else
        led = 1'b0;
end

endmodule

Test Bench code
module tb;

reg power_on;
wire led;

led_controller uut(
    .power_on(power_on),
    .led(led)
);

initial
begin
    power_on = 0;
    #10;

    power_on = 1;
    #10;

    power_on = 0;
    #10;

    $finish;
end

endmodule
