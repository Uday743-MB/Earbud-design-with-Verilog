Design code
module volume_controller(
    input vol_up,
    input vol_down,
    output reg [3:0] volume
);

always @(*)
begin
    if(vol_up)
        volume = volume + 1;
    else if(vol_down)
        volume = volume - 1;
end

endmodule
